fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
websockets==13.1
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.2
aiofiles==24.1.0
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
# Only used when TELEGRAM_WEBHOOK_ENABLED=true (app/telegram_bot.py) — the
# bot's own logic running inside this backend as a webhook route instead
# of a separate long-polling process. Same library bot/bot.py already
# depends on for its (unchanged) polling-mode deployment.
aiogram>=3.10,<4
